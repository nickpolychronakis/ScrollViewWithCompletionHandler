import XCTest
@testable import ScrollViewWithCompletionHandler

final class ScrollViewWithCompletionHandlerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ScrollViewWithCompletionHandler().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
